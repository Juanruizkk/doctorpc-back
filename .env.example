# Servidor
HOST=0.0.0.0
PORT=1337

# Seguridad - IMPORTANTE: Cambiar estos valores en producción
# Puedes generar valores aleatorios con: node -e "console.log(require('crypto').randomBytes(16).toString('base64'))"
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Base de datos
# Por defecto usa SQLite (archivo .tmp/data.db)
# Para producción, configurar PostgreSQL o MySQL:
# DATABASE_CLIENT=postgres
# DATABASE_HOST=127.0.0.1
# DATABASE_PORT=5432
# DATABASE_NAME=strapi
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=strapi
# DATABASE_SSL=false

# URL pública (opcional, para producción)
# URL=https://tu-dominio.com

# Admin panel
# NODE_ENV=development
